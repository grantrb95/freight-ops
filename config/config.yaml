# ============================================================================
# H-4 Strategic Solutions LLC - Freight Operations Configuration
# ============================================================================

# Company Information
company:
  name: "H-4 Strategic Solutions LLC"
  dot_number: "4486526"
  mc_number: "MC-1772833-C"
  
# Home Base Location
home_base:
  city: "Fort Gibson"
  state: "OK"
  coordinates:
    latitude: 35.7987
    longitude: -95.2500

# Equipment Configuration
equipment:
  truck:
    year: 2025
    make: "Dodge"
    model: "Ram 3500"
    mpg: 9
    fuel_tank_gallons: 32
    
  trailer:
    type: "gooseneck"
    subtype: "flatbed"
    length_ft: 40
    max_weight_lbs: 16500

# Operating Costs
costs:
  fuel_cost_per_gallon: 3.50
  cost_per_mile: 0.39
  insurance_monthly: 1500
  truck_payment_monthly: 1200
  trailer_payment_monthly: 400
  maintenance_reserve_per_mile: 0.10
  
# Rate Thresholds
rates:
  minimum_rate_per_mile: 2.00
  target_rate_per_mile: 2.50
  premium_rate_per_mile: 3.00
  max_deadhead_miles: 150
  max_deadhead_percentage: 20

# Operating Area
operating_area:
  preferred_states:
    - "OK"
    - "TX"
    - "AR"
    - "KS"
    - "MO"
    - "LA"
  max_radius_miles: 1000
  
# IFTA Tracking
ifta:
  enabled: true
  home_state: "OK"

# Load Board Preferences  
load_boards:
  dat:
    enabled: true
    search_radius_miles: 250
  truckstop:
    enabled: true
    search_radius_miles: 250

# Logging
logging:
  level: "INFO"
```

Save the file (Ctrl+S).

---

**Step 3: Create the .env file**
```
code .env
```

Paste this content:
```
# H-4 Strategic Solutions LLC - Environment Configuration
# KEEP THIS FILE SECRET - Never commit to Git

# LLM Provider API Keys
ANTHROPIC_API_KEY=your-key-here
OPENAI_API_KEY=your-key-here

# Google Maps API
GOOGLE_MAPS_API_KEY=your-key-here

# Load Board APIs
DAT_API_KEY=
TRUCKSTOP_API_KEY=

# Database
DATABASE_URL=sqlite:///./freight_ops.db
```

Save the file (Ctrl+S).

---

**Step 4: Commit the config changes**
```
git add config/config.yaml
git commit -m "Add H-4 Strategic Solutions business configuration"
git push